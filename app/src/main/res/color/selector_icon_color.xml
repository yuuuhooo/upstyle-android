<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 선택된 상태 (아이콘을 노란색으로 변경) -->
    <item android:color="@color/login_yellow" android:state_checked="true" />

    <!-- 선택되지 않은 상태 (아이콘을 검정색으로 변경) -->
    <item android:color="@color/black" />
</selector>
